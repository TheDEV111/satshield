export async function executeUnwind(vaultAddress: string, collateralAmount: bigint, minDebtRepay: bigint, currentLtvBps: number) {
  console.log(`Executing unwind for ${vaultAddress}...`);
  // Use @stacks/transactions to build and broadcast transaction
}
